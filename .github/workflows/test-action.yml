name: "Test action"

on:
  workflow_dispatch:
    inputs:
      blockchain-network:
        description: 'blockchain-network'
        required: true
        type: choice
        options:
          - a
          - b
          - c

jobs:
  test-job:
    runs-on: ubuntu-20.04
    steps:
      - name: hello
        run: echo hello