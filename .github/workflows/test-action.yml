name: "run always"

on: push

jobs:
  condition-test-1:
    if: ${{ "hui" == "alexanderrodnin"  && "ubuntu-22.04" || "ubuntu-20.04" }}
    runs-on: ubuntu-22.04
    steps:
      - run: echo "$RUNNER_ARCH"
      - run: echo "$RUNNER_OS"
  condition-test-2:
    if: ${{ "alexanderrodnin" == "alexanderrodnin"  && "ubuntu-22.04" || "ubuntu-20.04" }}
    runs-on: ubuntu-22.04
    steps:
      - run: echo "$RUNNER_ARCH"
      - run: echo "$RUNNER_OS"