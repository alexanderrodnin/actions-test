name: "run always"

on: push

jobs:
  condition-test-1:
    if: ${{ 'alexanderrodnin' == 'alexanderrodnin' }}
    runs-on: ubuntu-22.04
    steps:
      - run: echo "$RUNNER_ARCH"
      - run: echo "$RUNNER_OS"