name: "run always"

on: push

jobs:
  condition-test-1:
    strategy:
      matrix:
        runs-on:
          - ${{ '$GITHUB_ACTOR' == 'alexanderrodnin' && 'ubuntu-22.04' || 'macos-11' }}
          - ${{ '$GITHUB_ACTOR' == 'alexanderrodnin' && 'macos-11' || 'ubuntu-22.04' }}
    runs-on: ${{ matrix.runs-on }}
    steps:
      - run: echo "$RUNNER_ARCH"
      - run: echo "$RUNNER_OS"