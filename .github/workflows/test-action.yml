name: "run always"

on: push

jobs:
  select-env:
    runs-on: ${{ $GITHUB_ACTOR == "alexanderrodnin"  && ubuntu-22.04 || ubuntu-20.04 }}
    steps:
      - run: echo "$RUNNER_ARCH"
      - run: echo "$RUNNER_OS"